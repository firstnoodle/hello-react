const defaultTheme = require('tailwindcss/defaultTheme');

module.exports = {
  purge: [],
  theme: {
    extend: {},
  },
  variants: {
    shadow: ['focus', 'hover'],
    zIndex: ['focus', 'hover'],
  },
  plugins: [],
}
